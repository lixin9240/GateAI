# 数据库变更记录

## 2026-07-03 种子数据新增

**操作人**: wjc

| 变更类型 | 说明 |
|---------|------|
| 数据填充 | 新增 `ReservoirSeeder` — 3条水库（三峡、溪洛渡、向家坝） |
| 数据填充 | 新增 `EdgeNodeSeeder` — 6条边缘节点（含 online/offline/fault 状态） |
| 数据填充 | 新增 `AlarmSeeder` — 4条告警（含 unhandled/acknowledged/disposed 状态） |
| 注册Seed | `DatabaseSeeder` 增加调用顺序：RoleSeeder → UserSeeder → ReservoirSeeder → EdgeNodeSeeder → AlarmSeeder |

> **本次无表结构变更**（未修改任何 migration 文件，未新增/删除字段或表）