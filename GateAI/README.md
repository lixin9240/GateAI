- Request 层 → 只做参数验证 ✅
  - Controller 层 → 只做接收→调用 Service→返回 Result，每个方法 ≤5 行 ✅
  - Service 层 → 缓存、日志、第三方调用全部在此 ✅
  - 异常处理 → 使用 BusinessException + ResponseCode 枚举 ✅
  - 日志 → 结构化日志，按 channel 分离（business/exception/api） ✅
  - 响应格式 → 统一 {code, msg, data, success, trace_id} ✅